## 🚀 Estructura del proyecto

Astro busca archivos `.astro` en la carpeta `src/pages/`. Cada pagina se expone como una ruta diferente basado en el nombre del archivo.

En `src/components/`,Se ponen los componentes que luego usaremos en otros componentes o en las paginas / layouts
En `src/layouts/`,Se ponen plantillas de paginas para usar mas facilmente

## 🧞 Comandos

| Command           | Action                                    |
| :---------------- | :---------------------------------------- |
| `npm install`     | Instala las dependencias                  |
| `npm run dev`     | Inicia un servidor local `localhost:4321` |
| `npm run build`   | Construye la pagina en `./dist/`          |
| `npm run preview` | Revisa la pagina construida               |
